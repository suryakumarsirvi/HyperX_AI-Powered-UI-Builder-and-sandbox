Aaj ki class me hum AI-based functionality implement karenge jahan hamara platform AI tools ka use karke automatically UI generate karega.

Hum LLM provider ke liye Mistral AI ke high-capability models use karenge. Workflow orchestration ke liye LangGraph implement karenge, jisme default nodes `START` aur `END` honge. Is graph architecture me hum custom nodes add karenge:

* `intentAgent`
* `codeAgent`

Class me ye bhi discuss hua ki Microsoft ne internally employees ke liye Anthropic ke coding tools ka usage restrict/cancel kiya tha because operational cost kaafi high ho raha tha.

LLM integration ka base setup file:

```bash
ai/src/services/ai/llm.js
```

Uske baad hum API communication ke liye Axios setup file create karenge.

Phir hum:

* `intentAgent`
* `codeAgent`

implement karenge using proper `SystemInstruction`.

Humne multiple AI tools bhi create kiye jo agents ke through use honge.

Next step me hum ek dedicated graph file banayenge jisme complete LangGraph implementation hoga aur saare nodes orchestrate kiye jayenge.

Uske baad:

* AI service ka Docker image banega
* Deployment YAML likhenge
* Service YAML configure karenge
* Kubernetes cluster par deploy karenge
* End-to-end testing karenge

Development workflow ke liye:

```bash
skaffold dev
```

use karenge taaki Skaffold continuously build aur deploy handle kar sake.

Kyuki AI responses generate hone me time lag sakta hai, isliye gateway timeout increase karna padega. Ye configuration hum NGINX ke through manage karenge.


HTTP AGENT => createProxyMiddlware har request pr create ho rha hai isliye memory full hoti hai or memory leak chalu hoti hai isliye ham ek sandbox ke liye ek proxy create krte hai 


