agar 20 min tak apne user ka sandbox ideal rehta hai ya user koi interaction nhi krta toh ham usse delete kr denge

we'll use redis 

TTL - time to live 

download redis & redis insight

other databases use SSD/HARDDISK but redis stores data in RAM

RAM = read & write Speed very fast

redis is used for caching purpose

redis primary use is to reduce bandwidth and load to main database for frequent data 

TTL hame redis bhi deta hai or mongoDB bhi deta hai but ham redis isliye use kr rhe hai kyuki redis hame return krta hai ek eventafter deleting anything from storage of redis uing TTL. but mongoDB return nhi krta ki usne kuch delete kara hai

Persistance storage => ham directly mongodb, redis, postgreSQL ye sab images apne kuber cluster ke undar nhii chalana cahiye but recommanded nhi hai officaily by kubernetes community.

kyuki mongodb me apna data store hota hai or agar ham image ko pod banake run karege toh apna pod bydefaullt delete, restart, update hota rehta hai uske vjyse ip change hota hai new pods create hote rehte hai isse ye risk hai ki hamara sara data store jo tha sab udd jayega isliye recommanded nhi hota 

create redis account at : https://cloud.redis.io

create new db and click connect & click redis CLI and copy string :

<!-- redis url -->
[redis-cli -u ] <= ye nhi copy karna | sirf ye copy karna => redis://username:password@cub-lettuce-society-84229.db.redis.io:17778

create secret.yml file and add into .gitignore

install npm i ioredis & dotenv to start

we'll create 2 clients 
    - redis main client = used to write in redis
    - redis subscriber client = used to listen events from redis

see file /sandbox/server/config/redis.js

watch <= used to live logs of kuber cluster 


kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.12.1/deploy/static/provider/cloud/deploy.yaml